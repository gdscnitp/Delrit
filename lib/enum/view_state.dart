enum ViewState { Idle, Busy }

enum MobileVerificationState {
  SHOW_MOBILE_FORM_STATE,
  SHOW_OTP_FORM_STATE,
}

enum RideState {
  NO_RIDE,
  RIDE_SET,
  RIDE_CONFIRMED,
  RIDE_STARTED,
  RIDE_ENDED,
}
